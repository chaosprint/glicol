use super::super::{Pairs, Rule};

pub fn get_paras(paras: &mut Pairs<Rule>, num: usize) -> Vec::<String> {

    // for example: lpf -cut 100.0
    // ref and midi will be wrapped as seq, so it's not easy
    // paras -> 
};